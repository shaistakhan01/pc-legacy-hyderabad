# Server
PORT=5000
NODE_ENV=development

# Supabase (secret key — backend only, NEVER expose to frontend or commit this file with real values)
SUPABASE_URL=https://omwmkhrpzxbipundcxph.supabase.co
SUPABASE_SECRET_KEY=your-secret-key

# CORS — must match the deployed/local frontend origin exactly
CORS_ORIGIN=http://localhost:5173